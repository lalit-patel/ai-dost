import { FormEvent, useState } from "react";
import { useCreateExperience, useExperiences } from "../hooks/useProjectExperience";
export function ExperiencePage(){ const {data}=useExperiences(); const create=useCreateExperience(); const [form,setForm]=useState({company:"",role:"",duration:"",responsibilities:""});
const submit=(e:FormEvent)=>{e.preventDefault(); create.mutate(form); setForm({company:"",role:"",duration:"",responsibilities:""});};
return <div className="space-y-4"><h1 className="text-2xl font-semibold">Experience</h1><form onSubmit={submit} className="bg-white p-4 rounded shadow space-y-2"><input className="w-full border p-2" placeholder="Company" value={form.company} onChange={e=>setForm({...form,company:e.target.value})}/><input className="w-full border p-2" placeholder="Role" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}/><input className="w-full border p-2" placeholder="Duration" value={form.duration} onChange={e=>setForm({...form,duration:e.target.value})}/><textarea className="w-full border p-2" placeholder="Responsibilities" value={form.responsibilities} onChange={e=>setForm({...form,responsibilities:e.target.value})}/><button className="bg-slate-900 text-white px-3 py-1 rounded">Add Experience</button></form><div className="bg-white p-4 rounded shadow">{data?.map((x,i)=><div key={i} className="border-b py-2"><b>{x.company}</b> - {x.role} ({x.duration})</div>)}</div></div>; }
