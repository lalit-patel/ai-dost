import { FormEvent, useState } from "react";
import { useCreateProject, useProjects } from "../hooks/useProjectExperience";
export function ProjectsPage(){ const {data}=useProjects(); const create=useCreateProject(); const [form,setForm]=useState({title:"",description:"",techStack:""});
const submit=(e:FormEvent)=>{e.preventDefault(); create.mutate(form); setForm({title:"",description:"",techStack:""});};
return <div className="space-y-4"><h1 className="text-2xl font-semibold">Projects</h1><form onSubmit={submit} className="bg-white p-4 rounded shadow space-y-2"><input className="w-full border p-2" placeholder="Title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><input className="w-full border p-2" placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><input className="w-full border p-2" placeholder="Tech Stack" value={form.techStack} onChange={e=>setForm({...form,techStack:e.target.value})}/><button className="bg-slate-900 text-white px-3 py-1 rounded">Add Project</button></form><div className="bg-white p-4 rounded shadow">{data?.map((p,i)=><div key={i} className="border-b py-2"><b>{p.title}</b> - {p.techStack}</div>)}</div></div>; }
