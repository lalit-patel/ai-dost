server:
  port: 8083
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_dost
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
